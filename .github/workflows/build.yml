name: build and release

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    name: Build (Release)
    runs-on: windows-2022
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          msbuild .\Rubeus.sln /m /p:Configuration=Release /p:Platform="Any CPU"

      - name: Package
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Compress-Archive -Path .\Rubeus\bin\Release\* -DestinationPath .\dist\Rubeus-Release.zip -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Rubeus-Release
          path: dist/Rubeus-Release.zip
          if-no-files-found: error

  release:
    name: GitHub Release (tags)
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: Rubeus-Release
          path: dist

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/Rubeus-Release.zip
